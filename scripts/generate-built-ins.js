#!/usr/bin/env node
/**
 * Codegen for `src/plugins/built-ins.ts`.
 *
 * Walks `src/plugins/<bundle>/{plugin.json, <kind>s/<name>/index.ts}` and
 * emits a TypeScript module that statically imports every built-in
 * extension and exposes the same API as the hand-written predecessor
 * (`builtInBundles`, `builtIns()`, `listBuiltIns()`).
 *
 * Bundle metadata (id, granularity, description) is read from each
 * bundle's `plugin.json#/...` at codegen time and inlined; no JSON files
 * ship in `dist/`.
 *
 * Convention: the named export inside each `<kind>s/<name>/index.ts`
 * follows `camelCase(<name>) + <Kind>` (e.g. `tools-count` extractor
 * exports `toolsCountExtractor`). The codegen derives the import binding
 * from this rule.
 *
 * Run:
 *   node scripts/generate-built-ins.js
 *
 * Wired as `prebuild` in `src/package.json`; also exposed as
 * `built-ins:check` (drift-check) that fails CI when the committed
 * `built-ins.ts` is stale.
 */

import { existsSync, readFileSync, readdirSync, statSync, writeFileSync } from 'node:fs';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const HERE = dirname(fileURLToPath(import.meta.url));
const REPO_ROOT = resolve(HERE, '..');
const PLUGINS_ROOT = resolve(REPO_ROOT, 'src', 'plugins');
const OUTPUT = join(PLUGINS_ROOT, 'built-ins.ts');

/**
 * Canonical bundle order. Vendor providers FIRST so the kindRegistry
 * composer encounters them before the markdown fallback in `core`. The
 * matching directories under `src/plugins/<id>/` must all exist.
 */
const BUNDLE_ORDER = ['claude', 'gemini', 'openai', 'agent-skills', 'core'];

/**
 * Within a bundle, kinds register in this order so the resulting list
 * mirrors the legacy `built-ins.ts` snapshot ordering (providers first
 * within `core` so `kindRegistry` picks them up alongside vendors, then
 * extractors → analyzers → formatters → actions → hooks).
 */
const KIND_ORDER = ['provider', 'extractor', 'analyzer', 'formatter', 'action', 'hook'];

const KIND_TO_DIR = Object.fromEntries(KIND_ORDER.map((k) => [k, `${k}s`]));

/** TS interface name per kind, used for the bucket fields in `IBuiltIns`. */
const KIND_TO_TYPE = {
  provider: 'IProvider',
  extractor: 'IExtractor',
  analyzer: 'IAnalyzer',
  action: 'IAction',
  formatter: 'IFormatter',
  hook: 'IHook',
};

function camelCase(kebab) {
  const parts = kebab.split('-');
  return parts
    .map((p, i) => (i === 0 ? p : p.charAt(0).toUpperCase() + p.slice(1)))
    .join('');
}

function capitalize(s) {
  return s.charAt(0).toUpperCase() + s.slice(1);
}

function exportNameFor(name, kind) {
  return `${camelCase(name)}${capitalize(kind)}`;
}

/**
 * Emit a TypeScript single-quoted string literal. Escapes internal
 * single quotes and backslashes; preserves everything else verbatim
 * (template-literal characters like backticks are fine inside
 * single-quoted strings).
 */
function singleQuoted(str) {
  return `'${String(str).replace(/\\/g, '\\\\').replace(/'/g, "\\'")}'`;
}

function discoverBundle(bundleId) {
  const bundleDir = join(PLUGINS_ROOT, bundleId);
  const manifestPath = join(bundleDir, 'plugin.json');
  if (!existsSync(manifestPath)) {
    throw new Error(`Missing plugin.json for bundle '${bundleId}' at ${manifestPath}`);
  }
  const manifest = JSON.parse(readFileSync(manifestPath, 'utf8'));
  const extensions = [];
  for (const kind of KIND_ORDER) {
    const kindDir = join(bundleDir, KIND_TO_DIR[kind]);
    if (!existsSync(kindDir)) continue;
    const entries = readdirSync(kindDir).sort();
    for (const entry of entries) {
      if (entry.startsWith('.')) continue;
      const entryDir = join(kindDir, entry);
      let isDir = false;
      try {
        isDir = statSync(entryDir).isDirectory();
      } catch {
        continue;
      }
      if (!isDir) continue;
      const indexTs = join(entryDir, 'index.ts');
      if (!existsSync(indexTs)) continue;
      extensions.push({
        kind,
        name: entry,
        exportName: exportNameFor(entry, kind),
        importFrom: `./${bundleId}/${KIND_TO_DIR[kind]}/${entry}/index.js`,
      });
    }
  }
  return { manifest, extensions };
}

function render(bundles) {
  const lines = [];
  lines.push('// AUTO-GENERATED by scripts/generate-built-ins.js. Do not edit by hand.');
  lines.push('// Regenerate with `pnpm --filter @skill-map/cli build-built-ins`.');
  lines.push('// Source of truth: each bundle\'s plugin.json + filesystem layout under');
  lines.push('// src/plugins/<bundle>/<kind>s/<name>/index.ts.');
  lines.push('');
  lines.push('import type {');
  lines.push('  IAction,');
  lines.push('  IProvider,');
  lines.push('  IExtractor,');
  lines.push('  IFormatter,');
  lines.push('  IHook,');
  lines.push('  IAnalyzer,');
  lines.push("} from '../kernel/extensions/index.js';");
  lines.push("import type { Extension } from '../kernel/registry.js';");
  lines.push("import type { TGranularity } from '../kernel/types/plugin.js';");
  lines.push("import { bucketByKind } from '../kernel/util/bucket-by-kind.js';");
  lines.push('');

  // Imports (one per extension, alias-renamed so we can wrap with pluginId injection).
  for (const { bundleId, extensions } of bundles) {
    for (const ext of extensions) {
      lines.push(
        `import { ${ext.exportName} as _${ext.exportName} } from '${ext.importFrom}';`,
      );
    }
  }
  lines.push('');

  // Wrapped consts that inject pluginId from the bundle's plugin.json#/id.
  for (const { bundleId, extensions } of bundles) {
    for (const ext of extensions) {
      lines.push(
        `const ${ext.exportName} = { ..._${ext.exportName}, pluginId: '${bundleId}' };`,
      );
    }
  }
  lines.push('');

  lines.push('export interface IBuiltIns {');
  for (const kind of KIND_ORDER) {
    lines.push(`  ${KIND_TO_DIR[kind]}: ${KIND_TO_TYPE[kind]}[];`);
  }
  lines.push('}');
  lines.push('');
  lines.push(
    'export type TBuiltInExtension = IProvider | IExtractor | IAnalyzer | IAction | IFormatter | IHook;',
  );
  lines.push('');
  lines.push('export interface IBuiltInBundle {');
  lines.push('  id: string;');
  lines.push('  granularity: TGranularity;');
  lines.push('  description: string;');
  lines.push('  extensions: TBuiltInExtension[];');
  lines.push('}');
  lines.push('');

  lines.push('export const builtInBundles: IBuiltInBundle[] = [');
  for (const { bundleId, manifest, extensions } of bundles) {
    lines.push('  {');
    // Structure-as-truth: the bundle id comes from the directory name,
    // not from `plugin.json#/id` (the manifest no longer carries that
    // field). `granularity` defaults to `'extension'` when the manifest
    // omits it, matching the loader's runtime default.
    lines.push(`    id: '${bundleId}',`);
    lines.push(`    granularity: '${manifest.granularity ?? 'extension'}',`);
    lines.push(`    description: ${singleQuoted(manifest.description)},`);
    lines.push('    extensions: [');
    for (const ext of extensions) {
      lines.push(`      ${ext.exportName},`);
    }
    lines.push('    ],');
    lines.push('  },');
  }
  lines.push('];');
  lines.push('');

  lines.push('export function builtIns(): IBuiltIns {');
  lines.push('  const out: IBuiltIns = {');
  for (const kind of KIND_ORDER) {
    lines.push(`    ${KIND_TO_DIR[kind]}: [],`);
  }
  lines.push('  };');
  lines.push('  for (const bundle of builtInBundles) {');
  lines.push('    for (const ext of bundle.extensions) {');
  lines.push('      bucketBuiltIn(ext, out);');
  lines.push('    }');
  lines.push('  }');
  lines.push('  return out;');
  lines.push('}');
  lines.push('');

  lines.push('export function listBuiltIns(): Extension[] {');
  lines.push('  const out: Extension[] = [];');
  lines.push('  for (const bundle of builtInBundles) {');
  lines.push('    for (const x of bundle.extensions) {');
  lines.push('      out.push(toExtensionRow(x));');
  lines.push('    }');
  lines.push('  }');
  lines.push('  return out;');
  lines.push('}');
  lines.push('');

  lines.push('function bucketBuiltIn(ext: TBuiltInExtension, out: IBuiltIns): void {');
  lines.push('  bucketByKind(ext.kind, ext, {');
  for (const kind of KIND_ORDER) {
    lines.push(`    ${kind}: out.${KIND_TO_DIR[kind]},`);
  }
  lines.push('  });');
  lines.push('}');
  lines.push('');

  // Structure-as-truth: `stability` and `preconditions` were retired
  // with the manifest refactor; `description` is required on every
  // extension and survives as the only optional-display field on the
  // row (well, also `entry` which is loader-runtime, not surfaced here).
  lines.push('function toExtensionRow(x: TBuiltInExtension): Extension {');
  lines.push('  const row: Extension = {');
  lines.push('    id: x.id,');
  lines.push('    pluginId: x.pluginId,');
  lines.push('    kind: x.kind,');
  lines.push('    version: x.version,');
  lines.push('    description: x.description ?? \'\',');
  lines.push('  };');
  lines.push('  return row;');
  lines.push('}');
  lines.push('');

  return lines.join('\n');
}

function main() {
  const bundles = BUNDLE_ORDER.map((bundleId) => {
    const { manifest, extensions } = discoverBundle(bundleId);
    return { bundleId, manifest, extensions };
  });

  const content = render(bundles);

  const mode = process.argv[2];
  if (mode === '--check') {
    const current = existsSync(OUTPUT) ? readFileSync(OUTPUT, 'utf8') : '';
    if (current !== content) {
      process.stderr.write(
        `${OUTPUT} is stale. Run \`pnpm --filter @skill-map/cli build-built-ins\` and commit the result.\n`,
      );
      process.exit(1);
    }
    process.stdout.write(`${OUTPUT} is up to date.\n`);
    return;
  }

  writeFileSync(OUTPUT, content);
  const total = bundles.reduce((acc, b) => acc + b.extensions.length, 0);
  process.stdout.write(
    `${OUTPUT} written (${bundles.length} bundles, ${total} extensions).\n`,
  );
}

main();
