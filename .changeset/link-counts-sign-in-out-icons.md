---
"@skill-map/cli": patch
---

Swap the card-footer `linksIn` / `linksOut` icons from `pi-arrow-up` / `pi-arrow-down` to `pi-sign-in` / `pi-sign-out`. The door-with-arrow glyph reads as "things coming in / going out" without competing visually with the arrow markers Foblex paints on the graph's own edges.

## User-facing

**Footer link icons refresh** — the incoming / outgoing link counters on each node card now use door-style **sign-in / sign-out** icons instead of plain up / down arrows. The arrow shape is now exclusive to the graph's own edges, which keeps the visual vocabulary clearer.
