---
"@skill-map/web": patch
---

The marketing site's main screenshot now loads as a JPEG (`screenshot-1.jpg`) instead of the previous PNG, so the hero shots grid serves a lighter image with the same visual.
