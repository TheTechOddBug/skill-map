---
"@skill-map/web": patch
---

Sync the interactive roadmap timeline on the marketing site to the shipped state. Phase C (Real-time exploration) moves from planned to done, released as skill-map@0.87, with its milestones rewritten to what actually landed (live node activity, MCP client with live tool invocation, execution stats and spawns, live conversations; the persistent execution snapshot stays deferred). Phase D (LLM as an optional layer) becomes the in-progress phase. Content only, no layout or behaviour change.
