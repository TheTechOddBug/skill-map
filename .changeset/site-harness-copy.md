---
"@skill-map/web": patch
---

Refresh the marketing site copy and plugin examples. The hero, screenshots and CTA now read "agent harness" instead of "agent ecosystem", the Hook section counts ten subscribable events (was eight), and the six plugin code snippets (Provider, Extractor, Analyzer, Action, Formatter, Hook) are rewritten to the current folder-derived `export default` manifest shape.
