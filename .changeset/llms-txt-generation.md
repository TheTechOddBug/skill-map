---
"@skill-map/web": patch
---

Generate `llms.txt` (a curated index per llmstxt.org) and `llms-full.txt` (the concatenated normative prose contracts) on every site build from `spec/`, so an AI agent can ingest the skill-map spec from `skill-map.ai/llms.txt` and they can never drift from the source of truth.
