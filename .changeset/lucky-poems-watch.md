---
"@skill-map/web": patch
---

Add a Watch section to the landing page with the intro and masterclass videos, plus a secondary link from the hero. Both cards load behind a click-to-load facade, so no YouTube script or cookie reaches the visitor until they press play.
