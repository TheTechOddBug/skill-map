---
"@skill-map/web": patch
---

Relocate the NotebookLM episode source document from `context/` into `web/audio/`, next to the audio it generates, and rewrite it as a pain-first narrative (the tangle of agent files, the map that makes it predictable, the deliberately ignorant six-kind kernel, and a teaser for the opt-in LLM layer). De-drifted against the current model along the way (no global config layer, deterministic-only extractors and hooks).
