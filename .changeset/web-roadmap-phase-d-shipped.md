---
"@skill-map/web": patch
---

Sync the interactive roadmap timeline on the marketing site to the shipped state. Phase D (LLM as an optional layer) moves from current to done, released as skill-map@0.89, with its milestones rewritten to what actually shipped (pull-only job queue, AI summaries, AI findings with paired fixes and a full lifecycle, the UI findings workbench). Phase E (Surface and distribution) becomes the in-progress phase toward v1.0.0. Content only, no layout or behaviour change.
