/**
 * `<sm-tutorial-reminder-banner>` copy. Quick-test version: a centered
 * topbar nudge pointing first-time users at the interactive tutorial,
 * with a dismiss control.
 */
export const TUTORIAL_REMINDER_TEXTS = {
  body: 'New to skill-map? In an empty folder, run',
  command: 'sm tutorial',
  dismissAria: 'Dismiss the tutorial reminder',
} as const;
