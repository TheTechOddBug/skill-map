{
  "name": "@skill-map/web",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "description": "skill-map.dev public site source. Static HTML + i18n; deployed via Caddy/Docker.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "predemo:build": "rm -rf ../fixtures/demo-scope/.skill-map",
    "demo:build": "pnpm --filter ui build && node scripts/build-demo-dataset.js && cp -R ../ui/dist/ui/browser/. demo/ && node scripts/patch-demo-mode.js",
    "demo:serve": "node scripts/serve-demo.js",
    "build": "pnpm demo:build && node scripts/build-site.js",
    "docker:build": "docker build -t skill-map-test ..",
    "docker:run": "docker run --rm -p 8080:8080 skill-map-test",
    "docker": "pnpm docker:build && pnpm docker:run",
    "validate": "pnpm validate:compile",
    "validate:compile": "pnpm build"
  },
  "engines": {
    "node": ">=24.0"
  }
}
