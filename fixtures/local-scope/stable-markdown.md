---
title: Stable plain markdown
tags:
  - fixture
  - markdown
  - stable
  - root
---

# Stable plain markdown

Generic markdown node at the scope root. No Provider claims it via path conventions, so it falls back to the `core/markdown` classifier. The sidecar carries `stability: stable` to demonstrate the canonical happy-path shape for a non-provider node.

Related: [deep-path-markdown](test1/nombre/largo/muy/largo/veremos/que/pasa/deep-path-markdown.md).
Related: [otro](.gemini/skills/full-skill-gemini/SKILL.md).

@full-agent-claude

/stale-skill
