---
name: security-audit
description: Audit auth and input handling for common vulnerabilities.
---

# Security audit

Audit authentication and input handling against
[the architecture](../../../docs/architecture.md). Authored for Claude Code
under `.claude/skills/`, but OpenCode reads it too (Claude-compat), so under the
opencode lens it classifies as `opencode/skill`. The reviewer runs it on
auth-touching changes.
