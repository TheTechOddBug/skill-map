---
description: A leftover Claude Code slash command. OpenCode does not read Claude commands.
---

# Status

A Claude Code command under `.claude/commands/`. Under the **opencode** lens this
is plain `markdown`, NOT a command: OpenCode reads its own `.opencode/commands/`,
not `.claude/commands/`. Switch the lens to `claude` and it becomes
`claude/command`.
