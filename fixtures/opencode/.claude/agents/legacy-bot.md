---
name: legacy-bot
description: A leftover Claude Code subagent. OpenCode does not read Claude agents.
---

# Legacy bot

A Claude Code subagent under `.claude/agents/`. Under the **opencode** lens this
is plain `markdown`, NOT an agent: OpenCode's Claude-compat covers skills (and
CLAUDE.md), not `.claude/agents/`. Switch the lens to `claude` and it becomes
`claude/agent`.
