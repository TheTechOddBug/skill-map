---
name: changelog-entry
description: Draft a changelog entry from the merged changes.
---

# Changelog entry

Draft a user-facing changelog entry summarising what shipped. Lives under the
vendor-neutral `.agents/skills/` (the open standard OpenCode reads alongside its
own and the Claude-compatible homes).
