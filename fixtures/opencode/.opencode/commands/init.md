---
description: Scaffold a new empty page in public/ from the shared template.
---

Creates a blank page so you can start writing, then hand it to the
content-editor agent to fill in. Copy the `<!doctype html>` shell, link
`/style.css`, add an empty `<main>`, and link the new page from the home nav.
