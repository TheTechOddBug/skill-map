# Generated by `sm activity install opencode`: the in-process activity plugin
# (OpenCode's bridge-equivalent) and its npm deps. Regenerate rather than
# commit, matching the other providers' generated bridges. Both plugin dir
# forms auto-load.
plugin/
plugins/
node_modules/
package.json
package-lock.json
bun.lock
