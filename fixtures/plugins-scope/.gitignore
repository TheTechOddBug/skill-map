# Runtime artifacts regenerated by `sm scan` / `sm serve`, never committed.
# The committed source of this fixture is the node markdown plus the
# drop-in plugins under `.skill-map/plugins/` (re-included by the root
# .gitignore exception).
.skill-map/skill-map.db
.skill-map/settings.json
.skill-map/settings.local.json
.skill-map/backups/
