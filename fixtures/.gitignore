ignore
claude.old
ai-engineering
# Anchored to the top-level `fix:skills` scratch fixture only. Unanchored
# `skills` also matched every fixture's `.agents/skills/` directory, which
# silently dropped committed skill corpora (e.g. antigravity, codex).
/skills/
