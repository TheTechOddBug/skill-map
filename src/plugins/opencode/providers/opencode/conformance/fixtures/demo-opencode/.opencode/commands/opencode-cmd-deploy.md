---
description: Build and deploy the project.
agent: opencode-agent-review
---

An OpenCode custom command (lives under `.opencode/commands/`, plural), invoked
as `/opencode-cmd-deploy`. Builds and deploys the project.
