---
description: A Claude Code slash command. OpenCode does not read Claude commands.
---

# Claude-only command

Under the opencode lens this is plain markdown, NOT a command: OpenCode reads
its own `.opencode/commands/`, not `.claude/commands/`.
