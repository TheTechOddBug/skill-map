---
name: opencode-skill-lint
description: Lint the codebase and report style violations.
---

# OpenCode native skill

Lives under `.opencode/skills/` (OpenCode's own skill home). OpenCode loads it
on-demand through the native `skill` tool, not a slash command.
