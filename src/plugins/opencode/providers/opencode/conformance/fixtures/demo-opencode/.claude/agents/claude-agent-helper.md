---
name: claude-agent-helper
description: A Claude Code subagent. OpenCode does not read Claude agents.
---

# Claude-only agent

Under the opencode lens this is plain markdown, NOT an agent: OpenCode's
Claude-compatibility covers skills (and CLAUDE.md), not `.claude/agents/`.
