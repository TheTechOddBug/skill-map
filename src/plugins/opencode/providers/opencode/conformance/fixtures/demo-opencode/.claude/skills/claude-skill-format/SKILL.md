---
name: claude-skill-format
description: Format files per the project style guide.
---

# Claude-compatible skill

Authored for Claude Code under `.claude/skills/`, but OpenCode reads it too
(Claude-compat). Under the opencode lens this classifies as `opencode`/`skill`.
