---
name: goal
description: Named after the built-in /goal command, shadowed by the runtime.
---

# Goal

This skill is named `goal`, shadowing Antigravity's built-in `/goal`
slash command, so `core/name-reserved` flags it under the antigravity lens.
