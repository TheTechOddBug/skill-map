---
description: Named after the built-in /tasks command, shadowed by the runtime.
---

# Tasks

This workflow is named `tasks`, shadowing Antigravity's built-in `/tasks`
slash command, so `core/name-reserved` flags it (the reserved catalog
covers the `workflow` kind, not only `skill`).

1. List tasks

   `npm run tasks:list`
