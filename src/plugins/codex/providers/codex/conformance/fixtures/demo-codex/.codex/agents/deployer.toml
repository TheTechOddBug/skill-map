name = "deployer"
description = "Coordinates a release: build, review, ship."
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
nickname_candidates = ["Atlas", "Pilot"]
developer_instructions = """
Coordinate the release end to end.

1. Ask @builder.toml to compile the project.
2. Then ask @reviewer.toml to sign off on the diff.
3. Follow [the deploy guide](../../docs/deploy.md) before rolling out.

Release status board: https://status.example.com/releases
"""

[mcp_servers.docs]
url = "https://developers.openai.com/mcp"
