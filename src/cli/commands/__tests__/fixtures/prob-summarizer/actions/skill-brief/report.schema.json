{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skill-map.ai/spec/v0/test/skill-brief-report.schema.json",
  "title": "SkillBriefReport",
  "description": "Test-only report shape that extends the canonical summaries/skill schema. The $ref is the summarizer signal the record path detects: a completed record for this action lands a state_summaries row.",
  "allOf": [{ "$ref": "https://skill-map.ai/spec/v0/summaries/skill.schema.json" }]
}
