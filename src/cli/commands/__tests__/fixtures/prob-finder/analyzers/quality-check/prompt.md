Judge the quality of the skill below.

{{userContent}}

Return a JSON report with a `findings` array (type / severity / message
per entry), plus the top-level `safety` and `confidence` fields the
preamble requires.
