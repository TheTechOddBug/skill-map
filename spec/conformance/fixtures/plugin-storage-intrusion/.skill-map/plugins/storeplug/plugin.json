{
  "version": "0.1.0",
  "specCompat": "*",
  "catalogCompat": "*",
  "description": "Conformance fixture: same plugin as `plugin-storage-namespace` except its migration creates an UNPREFIXED table. The kernel MUST refuse it rather than silently rewriting the name, so the table the author reads in the migration is the table that exists on disk.",
  "storage": {
    "mode": "dedicated",
    "tables": ["rule_exceptions"],
    "migrations": ["migrations/001_initial.sql"]
  }
}
