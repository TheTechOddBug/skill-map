# Release notes

Body pinned upstream at acme/docs. The recorded remote copy under the
sibling `github-enrichment-recorded-remote/` fixture is byte-identical,
so the provenance verification lands on the happy path (verified true).
