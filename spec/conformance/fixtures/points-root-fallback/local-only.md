# Local only

Root-level file that the dot-slash mention in app/context/notes.md MUST
NOT resolve to: the explicit prefix pins resolution to the source file's
own folder, where no such sibling exists. (No backticked paths here, so
this body emits no links of its own.)
