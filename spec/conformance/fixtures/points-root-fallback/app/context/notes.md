# Notes

This is the progress tracker for stage S4 of
`_work_in_progress/plan.md` (the root-relative prose convention).

An explicit prefix declares file-relative intent and never falls back,
even though a file named local-only.md exists at the scan root (the
plain-prose mention on this line stays invisible to the extractor):
see `./local-only.md`.
