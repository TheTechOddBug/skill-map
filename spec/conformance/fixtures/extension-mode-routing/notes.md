# Routing notes

Plain markdown corpus for the `extension-mode-routing` conformance
cases. The scan classifies this file through the universal
`core/markdown` fallback so both the probabilistic submit (queued) and
the deterministic submit (refused, runs in-process) have a target node.
